# Must match SECRET_KEY in backend/app.py exactly
SECRET_KEY=change-this-secret

# Redis connection
REDIS_URL=redis://localhost:6379

# Port this sync server listens on
SYNC_PORT=3001

# Allowed CORS origin — use * for dev, your domain for prod
CORS_ORIGIN=*